// Outputs .ll & .o file
// .ll only contains declrations and usage, does not embed definitions from externs 
zig build-obj helper.zig --name helper -femit-llvm-ir

// Outputs .o
zig build-obj main.zig --name main  

// Linking
zig build-exe main.o helper.o --name finalExecutable

opt input.ll -strip-debug -o output.ll -S 
OR use -fstrip


